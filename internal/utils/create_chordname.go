package utils

func GetChordName(root string, suffix string) string {

	// if chordNames, ok := data.ChordNames[suffix]; ok {
	// 	if name, langOk := chordNames[lang]; langOk {
	// 		return fmt.Sprintf("%s %s", root, name)
	// 	}
	// }

	return ""
}
